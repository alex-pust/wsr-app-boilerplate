import React from 'react';
import { Card as CardWSR } from 'wix-style-react';

/**
 * A simple divider
 */
const CardDivider = () => {
  return (
      <CardWSR.Divider />
  );
}

export default CardDivider;
